# Default matrix

Every parameter the recorded spec (`probes/probe_out/openapi.json`, release
20260910.2) offers on the endpoints this server calls, and what the server
does with it. Generated by `scripts/default_matrix.py`; do not edit by hand —
`tests/test_defaults.py` fails when this file and the script disagree, and when
the spec lists a parameter without a decision.

The question behind every row: *if this is not sent, does the upstream default
narrow the answer?* Where it would, the parameter is sent (audit FID-001).

## `POST /search` — headers and SearchRequest body

| Parameter | Server |
|---|---|
| `Ocp-Apim-Subscription-Key` | sent: the subscription key (SecretStr, never logged) |
| `Authorization` | not sent — personalisation, needs an end-user token; off when absent |
| `Accept-Language` | sent: the tool's `lang` (de, fr, it, en); default would be de-CH |
| `Accept-Timezone` | not sent — changes how dates are written (offset), not which objects match. find_events filters by calendar day in Europe/Zurich via OData. The upstream default zone is unmeasured; pinning it waits for a live comparison. |
| `scope` | not sent — selects provider values for a named client target; no target applies, the provider's standard values are the ones wanted |
| `filters` | sent when needed: `geo.distance` radius and OData filters (length, ascent, difficulty, schedule) |
| `datasource` | not sent — a filter: absent means unrestricted |
| `project` | sent always: `[dsod-content]`; without it /search answers 401 |
| `sourcePartner` | sent when the caller asks for it (tool parameter); absent means unrestricted (`accessible`: `pi`, `okgo`) |
| `award` | not sent — a filter: absent means unrestricted |
| `campaignTag` | not sent — a filter: absent means unrestricted |
| `profileTag` | not sent — a filter: absent means unrestricted |
| `allTag` | not sent — a filter: absent means unrestricted |
| `category` | not sent — a filter: absent means unrestricted |
| `facets` | sent by explore_area and resolve_area: name, count, orderBy=count, orderDirection=desc |
| `scoringTag` | not sent — a filter: absent means unrestricted |
| `sourceId` | not sent — a filter: absent means unrestricted |
| `location` | not sent — a filter: absent means unrestricted |
| `hasGeoShape` | not sent — a filter: absent means unrestricted |
| `productAvailability` | not sent — a filter: absent means unrestricted |
| `action` | not sent — a filter: absent means unrestricted |
| `onlyWithAvailabilities` | not sent — acts only together with an availability filter, never set |
| `amenityFeature` | sent when the caller asks for it (tool parameter); absent means unrestricted (`amenities`) |
| `starRatingName` | not sent — a filter: absent means unrestricted |
| `starRatingValue` | sent when the caller asks for it (tool parameter); absent means unrestricted (`stars_min`) |
| `starRatingGarni` | sent when the caller asks for it (tool parameter); absent means unrestricted (`garni`) |
| `starRatingSuperior` | not sent — a filter: absent means unrestricted |
| `numberOfRooms` | not sent — a filter: absent means unrestricted |
| `numberOfBeds` | not sent — a filter: absent means unrestricted |
| `openingHoursSpecificationDayOfWeek` | not sent — a filter: absent means unrestricted |
| `priceRange` | sent when the caller asks for it (tool parameter); absent means unrestricted (`price_range`, sent as 1/2/3) |
| `standardPrice` | not sent — a filter: absent means unrestricted |
| `categoryTree` | sent when the caller asks for it (tool parameter); absent means unrestricted (`kind` for winter/cycling) |
| `suiCategoryTree` | not sent — a filter: absent means unrestricted |
| `tag` | not sent — a filter: absent means unrestricted |
| `containedInPlace` | sent when the caller asks for it (tool parameter); absent means unrestricted (`region`, resolved to an area id) |
| `addressLocality` | sent when the caller asks for it (tool parameter); absent means unrestricted |
| `addressPostalCode` | not sent — a filter: absent means unrestricted |
| `time` | not sent — a filter: absent means unrestricted |
| `length` | not sent as a body field — the tool sends it as OData in `filters` (probe 5.4) |
| `state` | not sent — a filter: absent means unrestricted |
| `ratingCondition` | not sent — a filter: absent means unrestricted |
| `ratingDifficulty` | not sent as a body field — the tool sends it as OData in `filters` (probe 5.4) |
| `elevationAscent` | not sent as a body field — the tool sends it as OData in `filters` (probe 5.4) |
| `elevationDescent` | not sent — a filter: absent means unrestricted |
| `elevationMinAltitude` | not sent — a filter: absent means unrestricted |
| `elevationMaxAltitude` | not sent — a filter: absent means unrestricted |
| `season` | sent when the caller asks for it (tool parameter); absent means unrestricted (`season_month`) |
| `type` | sent when the caller asks for it (tool parameter); absent means unrestricted |
| `combinedType` | not sent — a filter: absent means unrestricted |
| `combinedTypeTree` | not sent — a filter: absent means unrestricted |
| `leafType` | sent when the caller asks for it (tool parameter); absent means unrestricted |
| `searchText` | sent: the tool's `query` |
| `searchFields` | sent `name` for `match='name'`; omitted for `match='all'`, whose default is all fields (probe 4) — audit FID-004 |
| `select` | sent always: the fields a hit is built from (verified whitelist) |
| `currentPage` | sent always: the tool's `page` |
| `resultsPerPage` | sent always: the tool's `page_size`; default would be 10 |
| `orderBy` | not sent — relevance, or distance with scoringReferencePoint, is the order wanted |
| `scoringReferencePoint` | sent with `near`: ranks by distance |
| `onlySuggestions` | not sent — when absent the full result is returned |
| `facetOrder` | not sent — superseded by orderBy/orderDirection in each FacetRequest |
| `personalize` | not sent — personalisation, needs an end-user token; off when absent |
| `searchProfileName` | not sent — personalisation, needs an end-user token; off when absent |
| `viewId` | not sent — a view would pre-set parameters the server does not see |
| `scoringProfile` | not sent — spec: «if not set, the default scoring is applied» |
| `scheduleStart` | not sent — applied after the search, so `count` and paging miscount (discarded, see CHANGELOG); find_events sends the OData `schedule/any(...)` overlap in `filters` |
| `scheduleEnd` | not sent — applied after the search, so `count` and paging miscount (discarded, see CHANGELOG); find_events sends the OData `schedule/any(...)` overlap in `filters` |

## FacetRequest — each entry of `facets` in a search

| Parameter | Server |
|---|---|
| `key` | not sent — one facet per name; the answer is read by name |
| `name` | sent: one of the eight verified OData names; anything else is not sent |
| `responseName` | not sent — the default name is the one the reader looks up |
| `interval` | not sent — value facets only, no ranges |
| `values` | not sent — value facets only, no ranges |
| `selectValues` | not sent — every value, not a chosen subset |
| `filterValues` | not sent — every value, not a chosen subset |
| `count` | sent: 20 (explore_area), 30 (resolve_area — stated in the hint when reached) |
| `scope` | not sent — default `current`: values counted over the current result, as reported |
| `project` | not sent — the request itself is already scoped to dsod-content |
| `additionalType` | not sent — a filter: absent means unrestricted |
| `orderBy` | sent: `count` (the documented default, relied upon) |
| `orderDirection` | sent: `desc` (the documented default, relied upon) |
| `excludeRedundant` | not sent — default false: no value is dropped |

## `GET /vertices/{id}` — get_details

| Parameter | Server |
|---|---|
| `id` | sent: the identifier, after the shape gate (nothing is sent for an invalid one) |
| `Ocp-Apim-Subscription-Key` | sent: the subscription key (SecretStr, never logged) |
| `Accept-Language` | sent: the tool's `lang` (de, fr, it, en); default would be de-CH |
| `Accept-Timezone` | not sent — changes how dates are written (offset), not which objects match. find_events filters by calendar day in Europe/Zurich via OData. The upstream default zone is unmeasured; pinning it waits for a live comparison. |
| `scope` | not sent — selects provider values for a named client target; no target applies, the provider's standard values are the ones wanted |
| `ds-tagFilter` | not sent — trims the response; default `all` keeps every entry |
| `ds-containedInPlaceFilter` | not sent — trims the response; default `all` keeps every entry (probe finding 6: it is not a hit filter) |
| `ds-categoryFilter` | not sent — trims the response; default `all` keeps every entry |
| `categoryVersion` | sent: `sui` (vendor advice Aug. 2025; no measured effect) |
| `project` | sent always: `dsod-content` |
| `containedInPlace` | not sent — a filter: absent means unrestricted |
| `select` | not sent — the full object is read and trimmed server-side to about 8 KB |
| `photoSeason` | not sent — a filter: absent means unrestricted |
| `includeAllPhotos` | sent: `false` (documented default: low-confidence images skipped) |

## `GET` list endpoints — the list fallback (places, civicStructures, foodEstablishments, localbusinesses, lodgingbusinesses)

| Parameter | Server |
|---|---|
| `Ocp-Apim-Subscription-Key` | sent: the subscription key (SecretStr, never logged) |
| `Accept-Language` | sent: the tool's `lang` (de, fr, it, en); default would be de-CH |
| `Accept-Timezone` | not sent — changes how dates are written (offset), not which objects match. find_events filters by calendar day in Europe/Zurich via OData. The upstream default zone is unmeasured; pinning it waits for a live comparison. |
| `scope` | not sent — selects provider values for a named client target; no target applies, the provider's standard values are the ones wanted |
| `ds-tagFilter` | not sent — trims the response; default `all` keeps every entry |
| `ds-containedInPlaceFilter` | not sent — trims the response; default `all` keeps every entry (probe finding 6: it is not a hit filter) |
| `ds-categoryFilter` | not sent — trims the response; default `all` keeps every entry |
| `categoryVersion` | sent: `sui` (vendor advice Aug. 2025; no measured effect) |
| `category` | not sent — a filter: absent means unrestricted |
| `containedInPlace` | sent when the fallback has an area id; absent means unrestricted |
| `updatedSince` | not sent — a filter: absent means unrestricted |
| `datasource` | not sent — a filter: absent means unrestricted |
| `continuationToken` | sent when paging: the `nextPageToken` of the previous page |
| `top` | sent always: 1000 in the fallback (FALLBACK_TOP; the token is followed, as `top` is a wish), 200 otherwise; default would be 10 |
| `project` | sent always: `dsod-content`; without it an undocumented partner default applies |
| `select` | sent always: the list whitelist (about 1 KB per object instead of 9.5 KB) |
| `includeCount` | sent: `true` on the first page, `false` on the following ones |
| `identifiers` | not sent — a filter: absent means unrestricted |
| `photoSeason` | not sent — a filter: absent means unrestricted |
| `includeAllPhotos` | not sent — the list select carries no photo array |
| `descriptionMode` | not sent — default `full`: the description stays whole |
| `additionalType` | not sent — a filter: absent means unrestricted |
| `parentOrganization` | not sent — a filter: absent means unrestricted |

## Sent outside the spec's parameter lists

| Header | Server |
|---|---|
| User-Agent | `discover-swiss-mcp/<version>` with the repository URL, on every request |
| Accept | `application/json`, on every request |
| categoryVersion on /search | `sui` — sent with every request, listed in the spec for list and detail only |
